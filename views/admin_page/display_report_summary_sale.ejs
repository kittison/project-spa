<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>รายงานสรุปการขาย</title>
    <style>
        .report {
            width: 90%;
            padding: 20px;
            margin: 0 auto;
        }
        .header {
            padding: 10px;
            text-align: center;
        }
        .report-table {
            width: 100%;
            border-collapse: collapse;
        }

        .report-table th, .report-table td {
            border: 1px solid #ddd;
            padding: 10px;
        }
    </style>
</head>
<body>
    <div class="report">
        <div class="col-12">
            <div class="card card-body">
                <div class="header">
                    <h2>รายงานสรุปการขาย <%=value%></h2>
                </div>
                
                <table id="table" class="report-table" >
                    <thead>
                        <tr>
                            <th>วันที่</th>
                            <th>จำนวนรายการขาย</th>
                            <th>ราคารายการขาย</th>
                            <th>ส่วนแบ่งพนักงานบริการ</th>
                            <th>ราคาสินค้าที่ใช้ในบริการ</th>
                            <th>รายได้สุทธิ</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i of data) {%>
                            <tr>
                                <% let f_date = new Intl.DateTimeFormat('id-ID',{ day:'2-digit', month:'2-digit', year:'numeric'}).format(i.date) %>
                                <td><%= f_date%></td>
                                </td>
                                <td><%= i.count_sale%></td>
                                <td><%= parseFloat(i.total_sale).toFixed(2)%></td>
                                <td><%= parseFloat(i.total_employee_wage).toFixed(2)%></td>
                                <td><%= parseFloat(i.total_product_price).toFixed(2)%></td>
                                <td><%= parseFloat(i.total_sale - i.total_employee_wage - i.total_product_price).toFixed(2)%></td>
                            </tr>
                        <% } %>
                    </tbody>
                    <tfoot>
                        <tr>
                            <th style="text-align:end;" colspan="5">รวมทั้งหมด</th>
                            <td id="total"></td>
                        </tr>
                    </tfoot>
                </table>

                <script>
                    var table = document.getElementById("table"), sumVal = 0;
        
                    for(var i = 1; i < table.rows.length-1; i++)
                    {
                        sumVal = sumVal + parseFloat(table.rows[i].cells[5].innerHTML);
                    }
                    
                    document.getElementById("total").textContent = sumVal.toFixed(2);
                </script>
            </div>
        </div>
    </div>
</body>
</html>
